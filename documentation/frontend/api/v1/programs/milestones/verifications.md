## Verifications

`GET|POST /milestones/{milestone}/verifications`, `GET|PUT|PATCH|DELETE /verifications/{verification}`, `PATCH /verifications/{verification}/approve`, `PATCH /verifications/{verification}/reject`, and `PATCH /verifications/{verification}/request-audit`.

Applicants submit verification evidence; owners approve, reject with a reason, or request an audit.
