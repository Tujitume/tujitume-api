## Completions

`POST|GET /milestones/{milestone}/completions`, `PATCH /completions/{completion}/approve`, and `PATCH /completions/{completion}/reject`. Owners review completion evidence after milestone execution.
