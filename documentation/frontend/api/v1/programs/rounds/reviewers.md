## Reviewers

`GET /reviewer/assigned-rounds`, `GET|POST /rounds/{round}/reviewers`, `PATCH /reviewers/{reviewer}`, and `DELETE /reviewers/{reviewer}`. Owner actions manage assignments; reviewers may only view their assignments.
