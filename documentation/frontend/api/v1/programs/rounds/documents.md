## Round Documents

`POST|GET /applications/{application_id}/rounds/{round_id}/documents`, `PATCH /documents/{document_id}/verify`, `PATCH /documents/{document_id}/reject`, and `DELETE /documents/{document_id}`. Uploads use multipart form data; verified documents cannot be deleted.
