## Monitoring Checkpoints

`GET|POST /monitoring/applications/{app}/checkpoints`, `PATCH|DELETE /monitoring/checkpoints/{checkpoint}`. Owners define and maintain checkpoints; request data includes name, type, due date, and reporting requirements.
