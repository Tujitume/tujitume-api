## Site Visits

`POST /monitoring/checkpoints/{checkpoint}/site-visit/assign`, `POST /monitoring/site-visits/{visit}/submit`, `GET /monitoring/site-visits/{visit}`, and `GET /monitoring/reviewer/{reviewer_id}/site-visits`.

Only the assigned reviewer can submit a report.
